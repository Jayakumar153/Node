path join:
 * join path segment togetherr and behaviour Simply concatenates paths with correct / or \ separators
 Doesn’t care if input is relative

 path.resolve()
 *Resolves a path into an absolute one
 Starts from rightmost argument → goes left until an absolute path is found

 use join()when combining folder names
 use resolve() when you need to absolute path to file
 -------------------------------------------------------------------


* Synchronous vs Asynchronous fs Methods *

Synchronous:
Executes line by line — waits until file I/O completes

Asynchronous:

this process is dont wait untill process complete it run by run
------------------------------------------------------------------------

Why use nodemon in backend development?

Without nodemon:
Every time you edit your code, you must manually restart the server.

With nodemon:
It automatically restarts the server whenever files change.
